# Optional: required only for Elite Prospects PDF vision extraction.
OPENAI_API_KEY=
OPENAI_VISION_MODEL=gpt-5.6

# Optional: Poppler executable used only when rendering PDF pages for vision.
# Use `pdftoppm` when it is on PATH, or provide an absolute local path.
PDFTOPPM_PATH=pdftoppm
